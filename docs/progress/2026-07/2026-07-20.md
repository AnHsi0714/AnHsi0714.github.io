# 2026-07-20 ~ 2026-07-26

> 每月一個資料夾（`docs/progress/YYYY-MM/`），每週一個檔案，檔名為當週週一日期。每天工作完在當週檔案下面新增一個日期區塊。

## 2026-07-25（六）

### 新增 Reveal 元件，多頁面整合捲動淡入動畫

新增 `useScrollReveal` hook（`IntersectionObserver`，第一次進入畫面即觸發並 unobserve，不會捲上捲下反覆閃爍）與 `Reveal` 元件，整合進 Home、About、Articles、Dreams、Experience、Friends、Knowledge、Projects 多個頁面，區塊／清單項目捲動到畫面內時淡入。這個 commit 在本次對話開始前已完成，沒有第一手的決策脈絡可記錄，這裡僅依 diff 補上事實性摘要。

### Card hover 效果補齊手機版：捲動置中即亮起

`Card.module.scss` 的 `.hoverable` 外框亮起效果原本只寫在 `:hover` 偽類，手機沒有滑鼠指標、觸控不會觸發真正的 `:hover`，導致知識庫等頁面的卡片在手機上點進文章時完全看不到那個亮框反饋。

改成 `Card.tsx` 用 `IntersectionObserver` 偵測卡片是否進入畫面正中央的一條窄帶（`rootMargin: -45% 0px -45% 0px`），只在觸控裝置（`hover: none` 且 `pointer: coarse`，桌機滑鼠不受影響）啟用，是的話加上 `.centered` class；`Card.module.scss` 把 `.centered` 併進原本 `:hover` 的規則，視覺效果（外框亮起＋上移 2px）完全一致。邏輯寫在 `Card` 元件本身而非個別頁面，因此 Home、Articles、Projects、Knowledge、Playground，以及透過 `ExpandableCard` 的作品卡片，凡是用 `hoverable` 的地方手機版都自動套用，不用逐頁改。

### 站內返回維持原捲動位置

`App.tsx` 先前的捲動 effect（[2026-07-19 週已記錄的「查看完整經歷」修正](2026-07-13.md)）是路由一換頁就無條件 `window.scrollTo(0, 0)`，這個規則沒有區分「往前導覽」跟「上一頁返回」，導致從知識點文章點返回列表時也被強制跳回最上面，蓋掉瀏覽器原生會保留的捲動位置。

第一版改用 `useNavigationType()` 分流：`navigationType === 'POP'`（真正的瀏覽器上一頁／下一頁）時還原離開該頁當下記錄的捲動位置，其餘（`PUSH`／`REPLACE`）維持捲到頂端，用 module scope 的 `Map<historyKey, scrollY>` 搭配持續監聽 `scroll` 事件記錄。

但知識點／文章／專案詳細頁的「← 回列表」是一般 `<TextLink to="/knowledge">`（`Link` 點擊＝`PUSH`，不是 `POP`），套用後才發現這個最主要的返回入口完全沒被涵蓋到，點了還是跳頁首。因為單靠 `navigationType` 沒辦法區分「這個 PUSH 語意上等同上一頁」跟「NavBar 之類的全新導覽」，改用 `Link` 的 `state` 顯式標記意圖：`TextLink` 新增 `restoreScroll` prop，設定時導覽會帶 `state={{ restoreScroll: true }}`；`App.tsx` 額外維護一個 `Map<pathname+search, scrollY>`（`scrollPositionsByPath`），只要目的地帶著這個 state 就還原該路徑離開時的捲動位置，沒帶的一般連結（如 NavBar 的知識庫入口）仍然捲到頂端。三個列表頁（Knowledge／Articles／Projects）的「回列表」連結（含 not-found 情境，共 6 處）都補上 `restoreScroll`。

沒有採用 `navigate(-1)` 模擬「假返回」：那樣雖然能沿用既有的 POP 還原邏輯，但如果使用者是從別篇文章的相關詞條連結一路點進來（而不是直接從列表點進來），`navigate(-1)` 只會退回上一篇文章而不是列表本身，「回列表」卻沒回到列表，目的地本身就錯了，不只是捲動位置的問題。用 `state` 標記的作法保留了 `Link to="/knowledge"` 一定會正確落地在列表頁的保證。

## 2026-07-26（日）

### 新增文章範本（論文／技術／雜記／讀書心得）

`content/articles/*.md` 全部會被 [articles.ts](../../../src/lib/articles.ts) 掃描渲染成文章，所以範本不能放這裡，沿用 `docs/templates/project-template.md` 的既有慣例，新增 `docs/templates/{paper,tech,journal,book}-template.md` 四份，只留架構標題不留內容。

過程中發現 paper 範本的 `title:` 欄位如果用 `[年份] 文獻 [論文標題]` 這種方括號佔位符會被編輯器當成 YAML 陣列語法解析、後面接中文字直接報錯——frontmatter 是用類 YAML 格式解讀，`[` 開頭在真正的 YAML 裡會啟動 flow sequence。改用書名號《》包住論文標題（語意上也更貼切），其餘欄位的方括號佔位符也一併拿掉，四份範本都套用同一原則。另外每份範本開頭加了一行可刪除的 `> 撰寫期間：YYYY-MM-DD – YYYY-MM-DD`，因為 `Article` 的 `date` 欄位只有單一值且拿去排序（[articles.ts:46](../../../src/lib/articles.ts#L46)），不適合塞日期區間，寫作跨日的紀錄用內文一行帶過即可，不動 schema。

### 論文筆記文章整理：改檔名、格式、補圖、腳註、英文版

`123.md` 改名 `social-engagement-misinformation.md`；順手修了幾個格式問題：半形標點統一成全形、`19個月`補空格、四個遊戲操作項的強調格式原本只包住前兩項（不對稱的 `_..._`）統一成 `**粗體**`、`link: https://...` 純文字改成可點擊連結。

註記原本想用粗體區塊，後來考慮到 `remark-gfm`（[MarkdownContent.tsx](../../../src/components/MarkdownContent.tsx)）本來就支援 GFM 腳註語法，改用 `[^1]` / 文末 `[^1]: ...` 的正式腳註寫法。文章需要的遊戲介面截圖使用者已經放在 `content/articles/paper/HCI-p1.png`（這個路徑不會被打包進網站，只有 `public/` 會），搬到 `public/images/articles/social-engagement-misinformation/fakey-interface.png` 並補上具體圖說，原本的暫存資料夾一併清掉。新增 `social-engagement-misinformation.en.md` 英文版全文翻譯。

### 腳註連結不再強制開新分頁

[MarkdownContent.tsx](../../../src/components/MarkdownContent.tsx) 原本不分青紅皂白把所有 `<a>` 都加上 `target="_blank"`，套用腳註後才發現站內錨點（`remark-gfm` 產生的 `#user-content-fn-1` 這類 fragment）點下去等於在新分頁重新載入整篇文章，看不到跳轉效果。改成只有非 `#` 開頭（外部連結）才開新分頁，站內錨點交給 [App.tsx](../../../src/App.tsx) 既有的 hash 捲動邏輯處理，同頁跳轉。

### 修正 hash 捲動被 sticky NavBar 蓋住

腳註反向連結（remark-gfm 產生的 `↩`，指向段落中間的 `<a id="user-content-fnref-1">`）跳回去之後位置不對。第一版誤判是「行內元素用 `block:'start'` 對齊不好看」，改用 `getComputedStyle(el).display === 'inline'` 判斷、行內元素改用 `'center'`——結果使用者實測回報反向跳轉（研究方法段落）變正確了，但正向跳轉（跳到文末的腳註定義）反而看起來錯了，才發現真正原因是 [NavBar.tsx:42](../../../src/components/NavBar.tsx#L42) 用 `sticky top-0 z-50`：`scrollIntoView({block:'start'})` 把目標頂到畫面最上緣，不管是行內還是區塊元素，都會被蓋在浮動的 NavBar 底下，先前「行內用 center」只是恰好繞開了這個問題，沒有修到根本原因。

改回全站統一用 `block:'start'`，另外用 CSS `scroll-margin-top` 幫所有有 `id` 的元素（標題、腳註引用/定義）留出 NavBar 的淨空，數值直接讀 `NavBar.tsx` 用 `ResizeObserver` 算出的 `--nav-h` 變數（`scroll-margin-top: calc(var(--nav-h, 0px) + 1rem)`），不用另外猜一個容易過時的高度，也不必再分辨目標是不是行內元素。

`tsc --noEmit` 通過。

### 雜記文章整理：改檔名、格式、補圖、潤稿、英文版

沿用同一週稍早整理 `social-engagement-misinformation.md` 的流程，這次處理的是使用者直接把新內容寫進 `example-note.md`（原本的雜記範本佔位檔）留下的另一篇文章。`example-note.md` 改名 `exploring-hci.md`；`example-note.en.md` 沒有跟著改名，而是直接刪除——那份還是舊的 `(Example Note)` 佔位內容，跟新文章對不上，且站上目前沒翻譯的文章（如 social-engagement-misinformation）都是不放 `.en.md`、靠 [articles.ts](../../../src/lib/articles.ts) 的 fallback 邏輯（`enArticles.get(zh.slug) ?? zh`）自動顯示中文版，照同樣慣例處理，避免英文版顯示假內容。

圖片方面，使用者已經把 `guide.png`／`waiting.png`（CodePulse 的導覽提示、分析中等待畫面截圖）放進 `public/images/articles/`，對應文中「補 guide 圖」「補 waiting 圖」的預留位置；搬進 `public/images/articles/exploring-hci/` 子資料夾並改成語意化檔名 `codepulse-guide-tour.png`／`codepulse-loading-state.png`，比照 social-engagement-misinformation 的圖片組織方式。格式上補了 `excerpt`、把 `link: https://...` 改成正式腳註、`(...)` `"..."` `--` 等半形符號統一成全形、四個設計元素小標題加粗；潤稿修掉幾處口語重複的敘述。第二輪使用者又補了一張 `lock.png`（CodePulse 學習路徑節點圖，Stack/Queue/Linked List 都套用鎖頭樣式），放進同一個子資料夾，補在文末呼應內文提到的「鎖頭樣式與實際互動邏輯不一致」段落。

英文版 `exploring-hci.en.md` 是這次新增的全文翻譯，內容跟中文版一致，包含使用者仍在寫、結尾標了「未完待續」的狀態。
