# 2026-08-24 ~ 2026-08-30

> 每月一個資料夾（`docs/progress/YYYY-MM/`），每週一個檔案，檔名為當週週一日期。每天工作完在當週檔案下面新增一個日期區塊。

## 2026-08-24（一）

### 新增作品「星座碎片」sketch 移植

原稿「Celestial Fragments」用 `windowWidth`／`windowHeight` 畫滿整個瀏覽器視窗，靠 `windowResized()` 在視窗改變大小時重新 `generate()`；改寫成 instance mode 的 [celestialfragments.ts](../../../src/pages/gallery/sketches/celestialfragments.ts) 時，容器大小固定由外部傳入的 `width`／`height` 決定，不需要監聽 resize。多邊形的位置、半徑本來就是用 `width`／`height` 的比例算出來的，天生會跟著容器等比例縮放，只有描邊粗細、頂點圓點直徑、頂點漂移速度是原稿針對「視窗寬度 ~1912px」寫死的絕對像素值，統一乘上 `k = width / REFERENCE_WIDTH` 縮放。原稿只有 R 鍵手動重製，比照專案其他作品加上點擊畫布重製。

p5 的 `SCREEN`／`ADD`／`MULTIPLY` 混合模式常數在型別上是字串字面量（`p5.BLEND_MODE`），不是數字，`polygons` 陣列裡 `Polygon.mode` 欄位跟著改成 `p5.BLEND_MODE` 型別，`tsc --noEmit` 才過。

[index.ts](../../../src/pages/gallery/sketches/index.ts) 新增 `CELESTIAL_FRAGMENTS_ASPECT`（1912/897，跟現有 `WIDESCREEN_ASPECT` 1872/906 的截圖尺寸不同，另外訂常數）並註冊 `Celestial_Fragments` 這筆 sketch entry。`content/artworks.json` 裡這筆資料（標題「星座碎片」、`openProcessingUrl`、中文描述）已經先補好，這次另外補上 `content/artworks.en.json` 缺漏的英文條目（標題、描述皆為中文版翻譯）。

### 展牆進場填滿、往回捲夾值下限修正

[GalleryGrid.module.scss](../../../src/pages/gallery/GalleryGrid.module.scss) 的 `.wallRow[data-row="1"/"2"]` 用 `margin-left`（15%／7%）做磚牆式錯位排列，但單靠 CSS margin 會讓這兩列進場時左側露出一截空牆，要等自動橫移把 margin「走」完才會填滿。[useWallAutoScroll.ts](../../../src/pages/gallery/useWallAutoScroll.ts) 新增 `resetStagger`：在初始化跟每次真的需要重新分配作品時，直接把這兩列的位移量預先推進到等同 margin 的距離，讓填滿的錯位效果從第一幀就成立，不需要播放「走過去」的過程。滾輪往回捲的夾值下限也從寫死的 `0` 改成每列各自的錯位起點（`staggerOffsetsRef`），避免使用者手動捲回起點時又露出那截空牆。

### 新增「持續變動」篩選標籤

[sketches/index.ts](../../../src/pages/gallery/sketches/index.ts) 的 `SketchEntry` 新增 `animated?: boolean` 欄位，標了 8 件會自己持續演化、沒有收斂終點的作品（迷茫的臉、POP、爆發、魚生、色彩循環／v2／v3、星座碎片），逐一看過每個 sketch 的 `draw()` 邏輯篩出來的（迷幻綻放長完會凍結、RPS 分出勝負會停、金屬碰撞物理最終會堆疊靜止，這幾件不算）。[GalleryGrid.tsx](../../../src/pages/gallery/GalleryGrid.tsx) 的篩選標籤新增「持續變動」這個獨立分類（可以跟點擊重製等互動標籤並存），[GalleryDetail.tsx](../../../src/pages/gallery/GalleryDetail.tsx) 的操作提示句也會多一句「畫面會持續自行變化，靜靜看著就好」。[strings.ts](../../../src/i18n/strings.ts) 補上中英文對應字串。

### 修正切換語言時展牆重新洗牌、隨機換圖

中英文用兩份完全獨立的 JSON（`useLocalized` 直接切換物件參照），切換語言時 `artworks` 陣列連同裡面每個 artwork 物件都換成新參照，即使 slug 順序完全沒變。`useWallAutoScroll.ts` 原本只要偵測到 `artworks` 參照變了就整個重新分配版位（`distributeInitialRows`），而重新分配時每個作品的截圖是重新隨機挑一張，所以切語言就等於換圖。改成比對「作品 slug 序列」而不是物件參照本身：slug 順序沒變（多半是純切換語言）只用新增的 `remapArtworks` 把每個 slot 的 `artwork` 換成新語言版本，版位跟目前顯示的截圖都保持原樣；只有 slug 集合或順序真的變了（換了篩選條件、排序方式）才會真正重新洗牌版位、重挑隨機截圖。

### 新增作品「四角交織」sketch 移植

新增 [cornerconvergence.ts](../../../src/pages/gallery/sketches/cornerconvergence.ts)：原稿吃固定 910x910 正方形畫布，從四個角落各自往相鄰邊延伸線條。`space`（節點間距）跟 `lineWeight` 都是針對「910px 見方」寫死的絕對像素值，統一乘上 `k = size / REFERENCE_SIZE` 縮放。四個角落的畫線邏輯完全相同、只差起點跟終點公式，改寫時合併成一個迴圈跑一組設定陣列，省掉四段幾乎一樣的重複程式碼。

第一版直接照搬原稿 `for(i=0; i<=size; i+=space)` 配合 `countNode` 倒數計數的寫法，靠 910/70 剛好整除成 13 這個巧合讓每個角落畫滿 13 條線；但 `size` 換成外部傳入的容器寬度後不保證整除，浮點數累加也可能跟 `size` 對不上，這個巧合一旦跑掉，`countNode` 又是跨角落共用的倒數計數器，一個角落線條數不對，後面三個角落全部跟著歪掉，畫面比例就跑掉了。改成直接跑固定 13 次（`ORIGINAL_NODE + 1`）迴圈、用 index 算節點位置，不再讓「畫幾條線」取決於 `size` 能不能被 `space` 整除，不需要 `countNode` 那套權宜寫法，四個角落無論容器多寬都固定維持原稿的線條比例。原稿只有 S 鍵存檔，這裡加上點擊畫布重製（換一組新的深淺色背景跟配色）。

## 2026-08-29（六）

### 新增作品「海底城市 v2」sketch 移植

新增 [oceancityv2.ts](../../../src/pages/gallery/sketches/oceancityv2.ts)：城市背景在 `setup()` 時畫一次到離屏 `cityLayer`，`draw()` 每幀直接貼回，不用每幀重繪整座城市；魚群、飼料、氣泡的狀態都收在工廠函式的閉包裡，避免多個 sketch 實例共用到同一份全域陣列。互動是滑鼠拖曳灑飼料，魚會自動找最近的飼料游過去，吃到飼料後有機率長大、長到上限就不再繼續長。窗戶用窗框／未點燈玻璃／點燈玻璃三種顏色分工，避免糊成一片。尺寸常數多半是原稿針對「900px 見方」寫死的絕對值，統一乘上 `k = size / REFERENCE_SIZE` 等比例縮放。[sketches/index.ts](../../../src/pages/gallery/sketches/index.ts) 註冊這筆 sketch entry，`content/artworks.json`／`content/artworks.en.json` 補上對應標題、描述與中英文條目。

### 新增技術文章「HTTP Method 不只是 CRUD」

新增第一篇 `type: tech` 文章 [http-methods.md](../../../content/articles/zh/tech/http-methods.md)：改用「我想對 Resource 做什麼」的角度重新理解 GET/POST/PUT/PATCH/DELETE，取代死記查詢／新增／更新／刪除的對照方式，並釐清 PUT 跟 PATCH 的差異。中英文版本同步新增。

### sketches 檔案註解精簡（refactor）

30 個 sketch 檔案的說明註解統一精簡：拿掉「見對話紀錄」這類事後查不到來源的引用字眼，把偏口語、重複鋪陳的敘述壓縮成比較精簡的技術重點（原稿尺寸依據、reference width/size 縮放邏輯、點擊重製的綁定方式等），內容與行為都沒有改變，純粹是註解可讀性整理，[sketches/index.ts](../../../src/pages/gallery/sketches/index.ts) 也一併整理。

## 2026-08-30（日）

### Super Mario 專案頁補充操作說明與後續重構段落

[super-mario.md](../../../content/projects/zh/super-mario.md)（含英文版）的「操作」說明原本只是隨意條列，改成照實際按鍵動線重新排序（左右移動、上跳、下蹲、跑步射火球、開始/跳關、無敵、離開），並補上原本沒寫的下蹲跟離開鍵。新增「後續重構」小節，說明專案繳交後持續把動畫、碰撞判斷、平台移動等重複邏輯抽成共用函式，並統一敵人與道具的基底類別。

### Playground 小作品導覽統一改用 TextLink

[NameCardPage.tsx](../../../src/pages/playground/miniworks/NameCardPage.tsx)、[WeatherBoxPage.tsx](../../../src/pages/playground/miniworks/WeatherBoxPage.tsx)、[BrandStyleGuide.tsx](../../../src/components/BrandStyleGuide.tsx) 原本的「回列表」連結混用 `react-router-dom` 的 `Link` 或根本沒有，改成統一用專案自己的 [TextLink](../../../src/components/TextLink.tsx) 元件（帶 `restoreScroll`），跟站上其他返回連結的行為一致；`BrandStyleGuide` 之前甚至沒有回列表連結，這次一併補上。

### 新增作品「熔岩脈」sketch 移植

新增 [lavaveins.ts](../../../src/pages/gallery/sketches/lavaveins.ts)：地貌生成分兩階段，前 15 步用細胞自動機（依鄰居牆壁數收斂／消融）讓初始隨機牆面收斂成自然洞穴輪廓，這段期間地貌還沒穩定，先關閉滑鼠互動，避免使用者在牆壁還在跳動時就注入岩漿或挖穿地道；15 步之後切換成岩漿流體模擬，優先向下墜落，受阻就改斜向流動，再受阻就向兩側滿溢，且岩漿黏度高、每 2 幀才更新一次位置。截圖尺寸跟「星座碎片」共用同一個 `CELESTIAL_FRAGMENTS_ASPECT`（1912×897）常數，格子大小等其餘尺寸常數統一乘上 `k = width / REFERENCE_WIDTH` 等比例縮放。

### About 頁「學術成果」重寫

盤點後發現原本兩筆「學術成果」份量不對等：CodePulse 有指導教授、有投國科會大專生研究計畫，NLP 那筆只是課程期末專題，沒有任何外部檢核，掛在「成果」底下站不住腳。拿掉 NLP 那筆，換成目前躺在 `content/experience.json` 但完全沒在 About 頁露出的「2026 台灣棒球數據分析競賽」：自建爬蟲取 CPBL 逐球資料、自建打者決策模擬模型分析盜壘損益兩平門檻、提出「保留效應」修正傳統 RE24 的方法論貢獻，比課程作業更貼近「學術成果」。CodePulse 那筆另外補上量化結果（前後測使用者研究 n=56，學習信心顯著提升 p < 0.001），讓兩筆份量對齊。[About.tsx](../../../src/pages/about/About.tsx) 中英文同步修改。

討論中一併決定不動的兩件事：About 頁「經歷」摘要卡片跟 `content/experience.json` 全文兩邊分開維護的重複問題，先不做成自動衍生，因為 experience.json 的正式標題直接套用會失去 About 現有手調過的口語化摘要語感；生成視覺（Gallery）維持獨立頂層導覽，不併入 Playground，因為 About 頁研究興趣已經列了「Creative Coding / Creative Technology」這條線，獨立導覽才能跟這條研究興趣互相呼應，併入「還在玩的東西」定位的 Playground 反而會拉低這批作品的份量。

### 文章大小分類系統

盤點 12 篇文章的 `type` 與 `categories` 後，設計兩層分類：大分類（`ArticleSection`：academic／technical／reading／notes）＋小分類（原有的 `categories` 自由標籤）。大分類預設由 `type` 推導（paper／journal → academic，tech → technical，book／essay → reading），只有睡眠那篇 journal 內容跟其他三篇 HCI 反思性質差太多，額外在 frontmatter 明確覆蓋成 `section: notes` 獨立第四類，其餘文章不用逐篇手動填。順手合併「成長」「自我成長」這兩個意思重疊的小分類標籤（十八歲出門遠行那篇）。[content.ts](../../../src/types/content.ts) 新增型別，[articles.ts](../../../src/lib/articles.ts) 加上 `defaultSectionByType` 對照表跟覆蓋邏輯。

[Articles.tsx](../../../src/pages/articles/Articles.tsx) 篩選面板重新設計：大分類 Chip 列(全部/學術/技術/讀書心得/雜記)常駐在頁面上，選了大分類後篩選面板裡的小分類清單只列出該大分類底下實際出現的標籤，切換大分類會重置已選小分類。後來討論標籤數量以後可能一直長，與其做搜尋框或收合展開，決定直接把小分類 Chip 列表整個拿掉，改成既有的關鍵字搜尋框同時比對 `title` 跟 `categories`（例如輸入「生」會因為「生死議題」標籤搜到〈父後七日〉），介面更單純。

搜尋框的 placeholder 原本另外在 `articles` 底下加了專屬字串，後來發現這其實跟 Projects、Gallery 共用的 `common.titleKeywordPlaceholder` 是同一件事，只是後者寫死「輸入標題關鍵字」比較窄。改成把共用字串改名成 `common.keywordPlaceholder`、文案改成通用的「輸入關鍵字」，三個頁面都指回同一個字串，刪掉重複定義。

導覽列名稱中途試過把「文章」改成「思辨與洞察」，想強調學術筆記／技術文章／讀書心得三種文體代表的「研究生多維度思維」；但試完發現 nav 標籤最重要的是讓人一眼認出「這是什麼」，「思辨與洞察」要點進去才知道是文章列表，對第一次來的訪客（尤其是推甄審查者匆匆掃過的情境）反而增加理解成本，最後改回「文章」/"Articles"。多維度框架的說法留在頁面副標題（進頁面後解讀成本低，內容本身仍然成立）。

### 修正 featured 篩選時精選／全部區塊重複顯示

`featuredFilter` 篩到只顯示精選時，`filteredArticles`（全部區塊）跟 `featuredArticles`（精選區塊）內容會完全一樣，畫面上同一批文章／專案出現兩次。[Articles.tsx](../../../src/pages/articles/Articles.tsx) 跟 [Projects.tsx](../../../src/pages/projects/Projects.tsx) 都改成篩選條件已經是「只顯示精選」時不再額外渲染「精選」區塊標題，只留「全部」。Gallery 頁沒有 featured/all 雙區塊結構，不受影響。
