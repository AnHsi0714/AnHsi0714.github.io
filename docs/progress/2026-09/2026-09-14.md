# 2026-09-14 ~ 2026-09-20

> 每月一個資料夾（`docs/progress/YYYY-MM/`），每週一個檔案，檔名為當週週一日期。每天工作完在當週檔案下面新增一個日期區塊。

## 2026-09-16（三）

### 新增〈3CPEs：用具體物品學習抽象程式概念〉論文筆記

使用者提供了一份已經寫好、但夾雜多處 HTML 註解的草稿（`<!-- 在開頭補 License -->`、`<!-- 知識點 XXX -->`、`<!-- 補知識點link -->`、`<!-- 用表格呈現 -->` 等九處），逐一處理：查證原文作者（Ching-Ying Sung、Fu-Yin Cherng、Yi-Lun Chiu、Peng-Hsi Chen、Bing-Yu Chen，CHI EA 2024）補進「原文連結」那行；新建五個知識詞條（`3cpes`、`semi-structured-interview`、`snowball-sampling`、`paper-prototype`、`k-12`，`category: RESEARCH`），同步寫進 [knowledge.json](../../../content/knowledge.json)／[knowledge.en.json](../../../content/knowledge.en.json) 並在文中對應段落補 `data-term`；把閱讀心得結尾的對比敘述改成正式 markdown 表格。新增 [3cpes-concrete-programming-environments.md](../../../content/articles/zh/paper/3cpes-concrete-programming-environments.md)。

### License 規則釐清：只有用到圖片才需要標

草稿裡的 License 註解原本被誤判成要套用在這篇新文章上，後來使用者澄清規則是「有用到論文裡的圖片才需要加 License」，這篇沒有嵌圖所以不用。回頭比對站上另外兩篇 Dennis Wang 的論文筆記：`short-form-video-activity-sharing.md` 沒有圖片，不用加；`snappi-personal-data-stickers.md` 有兩張截圖（`sticker-creation-flow.png`、`goal-progress-color-fill.png`），照規則補上 **授權：CC BY 4.0**（中英文版都補）。

### 知識點 application 欄位改成 per-article 形式

`thematic-coding`、`quantitative-data` 這兩個詞被 SnapPI 和這篇 3CPEs 兩篇不相關的文章共用，但 `application` 原本是單一字串，寫死 SnapPI 情境的說明，導致從 3CPEs 文章點開會文不對題。查了 `src/types/content.ts` 的註解和 `resolveApplicationLines` 邏輯，確認 `application` 支援 `{ [文章 slug]: 該情境的說明 }` 這種寫法（沿用 `cosine-similarity` 節點的既有模式），改成 keyed 物件，中英文 knowledge.json 都同步改。

### 對照原文全文，修正一處數據誤讀、補上三段原文有寫但筆記漏掉的內容

貼出論文全文，逐段核對後：

- 修正一處誤讀：「從基礎的串列、迴圈到進階的條件和函式」裡的「串列」應該是「程式順序（sequence）」，原文兩輪課程設計教的是 sequence／loop（基礎）與 conditional／self-defined function（進階），串列完全沒被排進這兩輪。
- 補上原文獨立一節、筆記完全沒收錄的「課程設計案例」（P1 熱炒餐廳教序列／自訂函式、P3 教迴圈的兩個具體案例），新增 `### 課程設計案例` 小節。
- 把「作者將錄影內容做主題式分析」改寫成更準確的版本：三場工作坊、三種資料來源（課程與任務記錄、9 小時錄影、30 分鐘小組半結構式訪談）、雙編碼者（資工＋教育背景）協作分析，取代原本聽起來像單人作業的敘述。
- 回饋意見段落補上教師規劃把 CT Chef 排進既有課程序列（ScratchJr → CT Chef → Scratch）、P3 提到學生烹飪經驗有限可能影響參與度；結論補上 Piaget 具體運思期的理論依據。

### 潤稿：用字統一、標點、CT Chef 隱喻表格更正

「老師」／「教師」混用統一成「教師」；表格與內文的「副廚」「副主廚」不一致，原文確認 Table 1 寫的是 sous chef（副廚），改回「副廚」並在註腳補充副廚（自訂函式）跟主廚（main）的對應說明；補了兩處中英文之間缺空格、一處段落結尾漏句號。核對站上既有慣例後，把「原文連結」的連結文字從貼原文英文標題改成 frontmatter《》裡的中文譯名（跟其他三篇論文筆記一致），並把這個慣例寫進 memory（`feedback_paper_note_citation_zh`）。

「3CPEs」在簡介、研究方法、設計工作坊、研究發現、研究限制五個不同 H2/H3 小節都有出現，依照「每個小節第一次出現才標」的慣例，補齊原本只標了一處的缺漏。

### 閱讀心得對比表擴充

原本只有「研究對象」「工具形式」兩項，應要求從文章裡已經寫過的內容（結論段落的初學者／進階學習者設計方式差異、研究方法段的打地鼠對應陣列索引例子）整理出「目標學習者程度」「隱喻設計方式」兩個新項目；「評估方式」一項則是引用 CodePulse 使用者研究（Normalized Gain 前後測）在站上其他地方已公開的內容做補充。

### Template 更新：excerpt 改必填、拿掉「心得」分類

[paper-template.md](../../../docs/templates/paper-template.md) 的 `categories` 範例拿掉「心得」（跟其他論文筆記一致，改用領域標籤），新增 `excerpt` 欄位並標註必填；同時把這篇新文章、以及使用者自己順手統一的另外兩篇論文筆記標題格式（《...》後面拿掉冒號）都套用。

### 補英文版

新增 [3cpes-concrete-programming-environments.md（英文版）](../../../content/articles/en/paper/3cpes-concrete-programming-environments.md)，沿用 SnapPI 英文版的標題結構慣例（`## Introduction` / `## Methodology` / `## Findings` / `## Limitations` / `## Conclusion & Future Directions` / `## Reading Notes`）。跑過資料比對確認中英文 `data-term` id 集合與各自出現次數完全一致（`3cpes` ×5、`thematic-coding` ×2、`semi-structured-interview` ×2，其餘各 ×1）。

### 知識點 term 欄位英文排序檢查

使用者要求檢查所有知識點是不是都把英文放前面。逐一比對 `knowledge.json` 裡 68 個詞條的 `term` 欄位後，抓到 5 個中英文順序顛倒的：這次新建的 3 個（`semi-structured-interview`、`snowball-sampling`、`paper-prototype`）跟本來就存在、不是這次建的 2 個（`retrospective-pre-post`、`response-shift-bias`），全部改成「英文（中文）」順序。這個規則判斷屬於描述 `knowledge.json` 資料形狀的 codebase 慣例（不是針對使用者個人的協作偏好），寫進 [CLAUDE.md](../../../CLAUDE.md) 而不是記憶。

## 2026-09-18（五）

### 「個人網站」專案標記為完成

使用者確認可以把「個人網站」這個專案標記完成，[projects.json](../../../content/projects.json)／[projects.en.json](../../../content/projects.en.json) 的 `status` 從 `in-progress` 改成 `done`，`period` 從開放式的 `2026/06 ~` 收尾成 `2026/06 ~ 2026/09`；順便把 `desc` 欄位過時的「記錄人生、讀書、專案與夢想」（人生區已經被經歷頁取代）改成貼近現況的敘述。

### 朋友創作功能：決定移除，改建個人想法牆

使用者想把朋友創作（`/friends`，邀請碼兌換＋2D／3D 塗色編輯器）移除，因為「跟他人互動還是有點難」，改往「個人發想」方向。討論過程：

- 先給了四個「朋友互動類」替代方案（留言牆、共筆像素畫布、推薦牆、感言牆），使用者都不要，明確表態想要個人、不需要他人參與的方向。
- 提出把既有的 2D／3D 塗色編輯器技術（`/dev/creature`、`/dev/creature-builder`）轉成使用者自己的塗鴉/雕塑實驗場，重用大部分既有技術投資；使用者選了另一個方向：**想法牆**。
- 針對想法牆再細問三個關鍵決策：新增方式（Supabase 自寫、僅限本人，不走 git commit）、視覺呈現（便條紙／軟木塞板牆，不是規矩的卡片列表）、資料結構（純文字、完全自由，不加分類或關聯欄位，避免點子還很少時就過度設計）。
- 使用者問這個要不要開新分支：確認要開，且建議從 `main` 開新分支（例如 `feat/idea-wall`），不要從 `feat/content-pages` 開，因為兩者是不相關的關注點，各自獨立合併比較乾淨；這個決策沿用了 memory 裡「人生區改版時拆 `supabase-setup`／`friends-creator` 兩條分支」的先例。想法牆本身還沒開始寫程式，只是把方向定案。

### personal-website.md 專案文件同步更新

因為朋友創作即將被移除，[personal-website.md](../../../content/projects/zh/personal-website.md)（中英文都改）裡「朋友創作完整流程已完成但沒有入口」那段的後續說明，從「等另一條分支收斂後重新接上」改成「計畫移除，改建個人想法牆」；「之後想做的事」清單的第二項也同步從「朋友創作功能重新接上主要導覽入口」改成「移除朋友創作功能，改建個人想法牆」。深入的技術實作段落（邀請碼、RLS、Race Condition 等）維持原樣不動，因為程式碼還沒真的拔掉，等實際動工後再整段重寫，避免文件跟實際狀態脫節。

### 用暫時 Playwright 腳本重截三張過期截圖

使用者發現 `gallery.png`／`playground.png`／`dreams.png` 三張專案截圖是 2026-09-05 的舊圖，跟現在的畫面對不上（尤其 Playground 後來多了一張「生成視覺」卡片）。因為平常的慣例是不自己啟動 dev server／截圖自我驗證，先詢問使用者要怎麼處理；使用者授權寫一個暫時程式，唯一限制是不能影響到他自己開著的 5173／5174。做法：查了目前佔用中的 port（5173～5187 都有東西在跑），挑一個乾淨的 5199；寫一支一次性的 `scripts/tmp-screenshot.mjs`（讀取既有截圖的 PNG header 確認尺寸都是 1440×900，照同樣尺寸截圖），跑完之後把 dev server 關掉、腳本也刪掉，沒有留下任何暫時產物。

### 順便抓到並修正兩處跟畫面對不上的文件與文案

比對新截圖時發現：

- `personal-website.md` 的「藝術畫廊」小節標題、alt text、caption 都還寫著舊名字，但 `strings.ts` 裡這個頁面已經改名叫「生成視覺」（`nav.gallery`／`gallery.title`／`playground.galleryDesc` 都已經是新名字），中英文都改成新名字。
- `personal-website.md` 的 Playground 段落只列了三張卡片（夢想、小作品、組件預覽），漏掉後來新增的「生成視覺」卡片，補上變成四張。
- 追查同一個問題時，發現 `src/i18n/strings.ts` 裡 Playground 頁面的 subtitle（中英文）還寫著「朋友的創作」，但畫面上這張卡片早就被拔掉了，屬於實際站上文案本身的過時內容（不只是文件），一併改成提「生成藝術」。
- 使用者要求把其中一句「（原本叫「藝術畫廊」，後來改名）」的括號說明拿掉，順便確認生成視覺作品數量「30 件」是否正確——比對 `artworks.json` 陣列長度確認就是 30，不用改。

`npx tsc --noEmit` 全部改完後跑過一次確認沒有型別錯誤。
