# 2026-06-29 ~ 2026-07-05

> 每月一個資料夾（`docs/progress/YYYY-MM/`），每週一個檔案，檔名為當週週一日期。每天工作完在當週檔案下面新增一個日期區塊。

## 2026-06-29（一）

- 文章／專案新增站內詳細頁，不再只靠卡片預覽＋外部連結：
  - 路由新增 `/articles/:slug`（`ArticleDetail.tsx`）與 `/projects/:slug`（`ProjectDetail.tsx`），找不到對應 slug 時顯示 `EmptyState`。
  - `Articles.tsx`／`Projects.tsx` 的卡片改成可點擊連到詳細頁；`Projects.tsx` 卡片裡的 GitHub 外部連結維持獨立的 `<a>`（跟 `Link` 是兄弟元素，避免巢狀 `<a>`）。
- 詳細頁正文改用 markdown 渲染，支援文中穿插圖片／清單／引用（報導式排版）：
  - 新增套件 `react-markdown`、`remark-gfm`、`@tailwindcss/typography`，`index.css` 加上 `@plugin "@tailwindcss/typography"`。
  - 新增共用元件 `src/components/MarkdownContent.tsx`，包 `ReactMarkdown` + `remarkGfm`，套 `prose prose-neutral` class。
- 內容模型擴充：
  - `Article` 新增 `body`（全文 markdown）；`excerpt` 改為 frontmatter 可選覆寫，未填則自動從 `body` 萃取（去掉圖片/連結/標題語法後取前 100 字）。
  - `Project` 新增必填 `slug`，`projects.json` 兩筆資料補上對應 slug。
  - 把原本寫在 `lib/articles.ts` 裡的 frontmatter parser 抽到共用的 `src/lib/markdown.ts`（含新增的 `deriveExcerpt`），`lib/articles.ts` 改用共用版本。
  - 新增 `src/lib/projects.ts`，仿照 `lib/articles.ts` 用 `import.meta.glob` 讀 `content/projects/*.md`（檔名＝slug），沒有對應 `.md` 檔的專案詳細頁就只顯示 `desc`。
  - 新增範例正文 `content/projects/personal-website.md`，示範標題／段落／穿插圖片／清單／引用的排版效果（圖片路徑是 placeholder，尚未放實際截圖檔案）。
- 同步更新 `docs/ARCHITECTURE.md`（§3 內容分類、§8 目錄結構）。
- `tsc --noEmit`、`npm run build` 皆通過。

## 2026-06-30（二）

### 專案頁強化

- **`ProjectTag` union type**：新增封閉集合 `'NLP' | 'AI' | 'OOP' | 'GAME' | 'WEB' | 'DB' | 'VIZ' | 'EDU'`（仿 `BadgeVariant`，避免同義不同名）。`Project` interface 新增 `tags: ProjectTag[]`、`collaborators?: string[]`、`period?: string`、`advisor?: string`、`date: string`（YYYY-MM，供排序/篩選用）；`ProjectStatus` 新增 `'todo'`。
- **專案篩選器**：Projects 頁仿文章區加入篩選面板——標題搜尋、狀態 chips（todo/doing/done）、tag chips、起始/結束月份（`type="month"`）、排序（最新/最久），修正 `useMemo` 缺漏依賴導致篩選失效的 bug。Tag chips 顯示在卡片 badge 左側。
- **projects.json / MD 全部補齊**：5 個專案補上 `tags`、`collaborators`（隊友姓名已遮蔽中間字）、`period`、`date`；`projects.json` 為資料唯一來源，MD frontmatter 純文件用途。
- **ProjectDetail 顯示**：標題列右側顯示 tag chips + status badge；下方顯示期間、合作者、指導教授（`code-pulse` 獨有）。

### 專案 MD 格式統一

- 統一所有 `content/projects/*.md` 標題命名：`## 專案簡介`、`## 相關連結`、`## 系統架構`、`## 核心功能`、`## 心得`；`absa-wordcloud.md` 保留學術論文格式不動。
- 新增 `docs/templates/project-template.md` 範本（含一般專案格式 + 研究型替代結構），置於 `docs/templates/` 避免被 `import.meta.glob` 載入。

### About 頁面重構

- **版面**：左欄大標題「About Me」（`text-[10rem]`）透過負 margin（`-ml-[calc(50vw-50rem)]`）突破 `max-w-5xl` 左邊界延伸至頁面空白區，`sticky top-1/2 -translate-y-1/2` 固定在畫面垂直中央；右欄 `flex-1`。
- **新區塊**：研究興趣 Research Interests、學術成果 Academic Achievements、競賽與經歷摘要（4 筆，附「查看完整經歷 →」連結）。
- **Skills 重構**：拆成 `skillGroups`（語言 & 工具 / 專業方向），以 tag chips 排列，融入 Angular、HTML/SCSS、前端工程實踐（Scrum / Sprint Review / 設計文件）；興趣改為 4 欄 grid。

### 新增 /experience 時間軸頁面

- `src/pages/experience/Experience.tsx`：垂直時間軸，4 筆條目（全國技藝競賽、赴美見學、母校培訓、太陽鳥實習），各有彩色分類 tag（競賽/海外/教學/實習）、副標題、條列亮點。
- `router.tsx` 新增 `{ path: 'experience', ... }`，`NavBar.tsx` 新增「經歷」連結。

### Navbar fixed

- NavBar 加 `fixed top-0 inset-x-0 z-50 bg-[var(--color-bg)]`，滾動時固定在頂部不隨頁面捲動。`App.tsx` 的 `main` 加 `pt-20` 補高度偏移。

## 2026-07-04（六）

- 新增共用 `Chip` 元件（`src/components/Chip.tsx`）：`variant`（default/success/info/warn/danger，微透明背景）＋`size`（sm/md）兩個 props，取代各頁面各自手刻的 `<span className="rounded-full ...">` 標籤樣式。`ComponentsPreview` 新增對應示範區塊。
- 統一套用 `Chip` 到既有標籤：`Articles.tsx`（分類）、`Projects.tsx`／`ProjectDetail.tsx`（tag）、`About.tsx`（專業方向 skill tag、書卷標記）都改用 `Chip` 取代原本重複的內聯樣式。
- `Experience.tsx` 標籤重構：`tags` 從純字串陣列改成 `{ label, variant }[]`，移除原本用 `Record<string, string>` 手動對應顏色的 `tagColor`，改用 `Chip` 的 `variant` 直接決定配色；新增一筆「教育大數據微學程」研究成果經歷，並新增「其他獎項」次要條列區塊（`secondaryEntries`，含程式設計競賽、英文菁英盃、PVQC 認證等 3 筆）。
- About 頁補強：
  - 學歷區新增可展開的歷年成績（`academicRecord`：學期／名次／平均／是否書卷），預設收合，點擊以 `faChevronRight` 圖示旋轉展開；書卷學期以 `Chip variant="warn"` 標示。
  - 研究興趣改為分層呈現（`層級：核心 / 支撐方法 / 應用領域`），取代原本扁平的標籤清單。
  - Skills「專業方向」項目內容微調（拿掉演算法設計、互動視覺化重新歸類、新增資料分析與探索），改用 `Chip` 顯示。
- `Badge.tsx` 順手跑過 prettier 格式化（雙引號、換行風格），無邏輯變更。
- **Experience 資料改走 JSON**：仿照 `projects.json`／`dreams.json` 的模式，把 `Experience.tsx` 裡寫死的 `entries`／`secondaryEntries` 抽到 `content/experience.json`；`types/content.ts` 新增 `ExperienceTag`（`variant` 沿用 `ChipVariant`）／`ExperienceEntry`／`SecondaryExperienceEntry` 型別，`Experience.tsx` 改為直接 import JSON 並用 `as` 轉型，渲染邏輯不變。`tsc --noEmit` 通過。
- 調整 `content/dreams.json`：「在 2026 撰寫一篇讀書心得」「在 7 月底前寫一篇文章」補上 `progress`（0/1 篇），並微調文字。

## 2026-07-05（日）

- **畫廊詳細頁 `/gallery/:slug` 正式實作**（原本只是 placeholder 文字），照 `docs/ARCHITECTURE.md` §6 的 p5.js instance mode 設計串接第一件作品當範例：
  - 新增依賴 `p5`（純 JS，鎖 `1.x` 避開才剛推出不久、可能有破壞性變更的 `2.x`）與型別套件 `@types/p5`。
  - 新增 `src/pages/gallery/sketches/entanglement.ts`：把「纏繞」原本的 OpenProcessing global-mode 程式碼改寫成 instance mode（`(p) => { p.setup = ...; p.draw = ... }`），`size` 從寫死的 800 改成參數傳入，讓畫布能配合展場容器縮放。
  - 新增 `src/pages/gallery/sketches/index.ts`：`slug -> sketch factory` 的對照表，之後每移植一件作品就加一筆映射；沒有對應項目的作品維持原本的靜態截圖展示。
  - `GalleryDetail.tsx`：依 slug 查有沒有對應 sketch，有的話動態 `import('p5')` 後用 `new p5(...)` 掛載到 `containerRef`，卸載時呼叫 `instance.remove()`；沒有的話退回顯示第一張截圖。
  - 新增 `GalleryDetail.module.scss`：套用 §6.1 的「展覽感」聚光燈視覺——沿用列表頁 `GalleryGrid.module.scss` 的 full-bleed 負 margin 手法（`margin-top: -5rem` + `margin-bottom: -2rem` 抵銷 `main` 的 `pt-20`/`pb-8`），加上以 canvas 為中心的 radial-gradient 聚光燈、vignette、返回按鈕改成半透明藥丸狀樣式。
  - 過程中修掉兩個一開始沒注意到的定位問題（細節見 §6.1 實作備註）：返回按鈕一開始被固定 NavBar 蓋住（absolute 定位基準點也被負 margin 一起往上移了，要加回 5rem 才對）；`.stage` 少複製 `margin-bottom: -2rem`，導致頁面多出 2rem 可以捲動、底部露出一截白色背景。
  - 詳細頁背景色改成日夜模式各自一套（`--stage-bg`）：日間用柔和深灰 `#2e2e35`，避免淺色 NavBar 直接撞上純黑展場；夜間才是近黑 `#0a0a0d`。
  - `tsc --noEmit` 通過。
- 同步更新 `docs/ARCHITECTURE.md`（§3 藝術畫廊 metadata 澄清 sketch 直接沿用 artwork 的 slug、§6.1 補上實作備註）。
- **再移植 4 件作品**：`sketches/moontain.ts`（山與月）、`sketches/pop.ts`（POP）、`sketches/tentacle.ts`（觸手）、`sketches/audio.ts`（音頻），皆同樣改寫成 instance mode；「山與月」跟「觸手」原稿只在 `setup()` 畫一次靜態構圖、沒有 `draw()`，port 時保留同樣行為（不额外加 loop）。
  - **`sketches/index.ts` 重構**：發現「POP」「觸手」「音頻」原稿是拿 `windowWidth`/`windowHeight` 畫滿整個瀏覽器視窗（寬螢幕比例），跟「纏繞」「山與月」原本寫死的正方形 `size×size` 不一樣，不能全部套同一個正方形容器。改成 `SketchEntry { factory, aspect }`：`aspect` 取自對應截圖的實際尺寸（`public/images/gallery/POP0.jpg` 等三張都是 1872×906，`moontain0.jpg` 是 800×800），忠實還原原始構圖比例。
  - **`GalleryDetail.module.scss` 同步調整**：`.spotlight` 改回只負責置中、不寫死形狀；寬高比交給 `.canvasHost` 的 CSS 變數 `--sketch-aspect`（由 `GalleryDetail.tsx` 透過 inline style 帶入），`width: min(900px, 88vw, calc(68vh * var(--sketch-aspect)))` 同時被寬度上限跟高度上限夾住；沒有 sketch 的作品（`.image` fallback）改用 `max-width`/`max-height` 讓瀏覽器照圖片原始比例算尺寸，不再是正方形裁切。
  - `tsc --noEmit`、`npm run build` 皆通過（`p5` 被 rollup 拆成獨立 chunk，確認只有進到 `/gallery/:slug` 才會載入）。
  - 同步更新 `docs/ARCHITECTURE.md` §6.1，補上 `aspect`/`--sketch-aspect` 這個設計決定的說明。
- **修正「POP」「觸手」「音頻」構圖比例錯亂的 bug**：實際接上畫布後發現觸手看起來只剩一角、音頻兩側被裁掉。根本原因是原稿裡不少常數（觸手的點位間距最遠到 1700px、半徑 150~200px；音頻的半徑成長 10~140px、水平偏移 20~35px/次；POP 的泡泡大小 30~100px、位移 ±20px）是針對「視窗寬度 ~1872px」寫死的絕對像素值，不是 width 的比例——展場畫布通常遠小於 1872px，直接照搬會讓大半構圖畫到看不見的畫布外，或是同樣的絕對位移換算下來畫面內能看到的比例變小、更像被裁掉。三個檔案都補上 `const k = width / 1872`，把這類絕對像素常數統一乘上 `k` 等比例縮小，讓構圖不管畫布多大都維持原本的相對比例；`tsc --noEmit`、`npm run build` 皆通過。
- **artworks.json 加上 `openProcessingUrl`**：每件作品補上 OpenProcessing 原稿連結，`GalleryDetail.tsx` 詳細頁新增「在 OpenProcessing 查看原稿 ↗」外部連結。
- **再移植 4 件作品**：`sketches/bat.ts`（蝙蝠）、`sketches/prism.ts`（稜鏡）、`sketches/tri.ts`（TRI）、`sketches/trii.ts`（TRII），皆為寬螢幕滿版作品；TRII 原稿吃 `windowWidth/2 x windowHeight`，`index.ts` 另設 `TRII_ASPECT = 1872 / 2 / 906`。
  - **既有作品的點擊重製改綁 canvas 元素**：原本用 `p.mousePressed`（整個 document 點到哪都觸發，點返回按鈕也會重製），統一改成 `canvas.mousePressed(...)`，只有點在畫布內才重新產生構圖。
